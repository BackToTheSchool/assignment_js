#include<stdio.h>
#include<string.h>
#include<windows.h>

#define CRT_SECURE_NO_WARNNINGS
/*
int main() {
	char quote[200], name[20];
	printf("What is the quoute? ");
	fgets(quote, sizeof(quote), stdin);
	printf("Who said it? ");
	fgets(name, sizeof(name), stdin);
	quote[strlen(quote) - 1] = '\0';
	name[strlen(name) - 1] = '\0';

	printf("%s said, \"%s\"",name,quote);


system("pause");

}*/