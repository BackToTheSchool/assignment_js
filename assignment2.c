#include <stdio.h>
#include <string.h>
#include <windows.h>
/*int main() {
	char string[20];
	printf("Put the string\n");
	fgets(string, sizeof(string), stdin);
	while (string[0] == '\n')
	{
		printf("Put the string\n");
		fgets(string, sizeof(string), stdin);
	}
	string[strlen(string) - 1] = '\0';
	printf("Your string is %s and length of your string is %d",string,strlen(string));
	system("pause");
	return 0;
}*/